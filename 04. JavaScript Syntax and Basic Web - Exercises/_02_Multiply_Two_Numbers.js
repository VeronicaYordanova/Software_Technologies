function multiplyTwoNumbers(numbers) {
    let numberX = Number(numbers[0]);
    let numberY = Number(numbers[1]);
    let multiply = numberX * numberY;
    console.log(multiply);
}
multiplyTwoNumbers(['2', '3'])